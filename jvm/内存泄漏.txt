内存泄漏是指存在着强引用，却又不被使用或丢失它的地址，导致不会被回收。
内存泄漏一般会存在老年代，因为gc后，一直存在，年龄大了就会进入老年代。
通过jvisual查看内存情况，如果老年代持续增长。基本可以算是内存泄漏。
这时候可以通过jmap -dump:format=b,file=xx.hprof 生成dump文件
通过eclipse的memory anylize去分析。看以看到具体是哪个对象存在的较多。